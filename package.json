{
  "name": "lambda-durable-functions",
  "version": "1.0.0",
  "description": "AWS Lambda Durable Functions examples with CDK deployment",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:cdk && npm run build:functions",
    "build:cdk": "cd cdk && npm run build",
    "build:functions": "npm run install:all",
    "deploy": "cd cdk && cdk deploy",
    "deploy:all": "cd cdk && cdk deploy --context functions=all",
    "deploy:approval": "cd cdk && cdk deploy --context functions=approval-workflow",
    "deploy:order": "cd cdk && cdk deploy --context functions=order-processing",
    "deploy:batch": "cd cdk && cdk deploy --context functions=batch-processor",
    "deploy:dev": "cd cdk && cdk deploy --context stage=dev",
    "deploy:prod": "cd cdk && cdk deploy --context stage=prod --context functions=all",
    "destroy": "cd cdk && cdk destroy",
    "test": "npm run test:cdk",
    "test:cdk": "cd cdk && npm test",
    "lint": "eslint . --ext .js,.ts,.mjs",
    "format": "prettier --write .",
    "synth": "cd cdk && cdk synth",
    "synth:all": "cd cdk && cdk synth --context functions=all",
    "diff": "cd cdk && cdk diff",
    "diff:all": "cd cdk && cdk diff --context functions=all",
    "install:all": "npm run install:order && npm run install:approval && npm run install:batch",
    "install:order": "cd durable-order-processing && npm install",
    "install:approval": "cd durable-approval-workflow && npm install",
    "install:batch": "cd durable-batch-processor && npm install"
  },
  "keywords": [
    "aws",
    "lambda",
    "durable-functions",
    "cdk",
    "serverless",
    "workflow"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
